<div id="contribute">
	<div id="images">
		<% @images.each do |img| %>
			<%= image_tag(img.picture(:small), :id => "#{img.word}", :class => 'image')%>
		<% end %>
		<br style="clear:both;"/>
	</div>
</div>